# typed: false
# frozen_string_literal: true

# This file was generated by GoReleaser. DO NOT EDIT.
class ConductorKit < Formula
  desc "Global skills pack and Go helper for Codex CLI and Claude Code"
  homepage "https://github.com/Skyline-23/conductor-kit"
  version "0.1.7"
  license "MIT"

  on_macos do
    if Hardware::CPU.intel?
      url "https://github.com/Skyline-23/conductor-kit/releases/download/v0.1.7/conductor-kit_0.1.7_darwin_amd64.tar.gz"
      sha256 "2dfdc3a06cc0273260e70d78bd5ccfe573cdd62fd2c2935e87b4166fbff141c4"

      def install
        bin.install "conductor"
        (bin/"conductor-background-task").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-task "$@"
        EOS
        (bin/"conductor-background-output").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-output "$@"
        EOS
        (bin/"conductor-background-cancel").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-cancel "$@"
        EOS
        (bin/"conductor-background-batch").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-batch "$@"
        EOS
        chmod 0755, [bin/"conductor-background-task", bin/"conductor-background-output", bin/"conductor-background-cancel", bin/"conductor-background-batch"]
        pkgshare.install "skills", "commands", "config"
      end
    end
    if Hardware::CPU.arm?
      url "https://github.com/Skyline-23/conductor-kit/releases/download/v0.1.7/conductor-kit_0.1.7_darwin_arm64.tar.gz"
      sha256 "734b32a33f0b87f88aa3250c41bf7da51e6b8a897e0bbe4dc353b9265131299a"

      def install
        bin.install "conductor"
        (bin/"conductor-background-task").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-task "$@"
        EOS
        (bin/"conductor-background-output").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-output "$@"
        EOS
        (bin/"conductor-background-cancel").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-cancel "$@"
        EOS
        (bin/"conductor-background-batch").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-batch "$@"
        EOS
        chmod 0755, [bin/"conductor-background-task", bin/"conductor-background-output", bin/"conductor-background-cancel", bin/"conductor-background-batch"]
        pkgshare.install "skills", "commands", "config"
      end
    end
  end

  on_linux do
    if Hardware::CPU.intel? && Hardware::CPU.is_64_bit?
      url "https://github.com/Skyline-23/conductor-kit/releases/download/v0.1.7/conductor-kit_0.1.7_linux_amd64.tar.gz"
      sha256 "be5fa998e14d2eae79f1e0b0d8ded565bf26948717cca20cea5cdbf17b50a05b"
      def install
        bin.install "conductor"
        (bin/"conductor-background-task").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-task "$@"
        EOS
        (bin/"conductor-background-output").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-output "$@"
        EOS
        (bin/"conductor-background-cancel").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-cancel "$@"
        EOS
        (bin/"conductor-background-batch").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-batch "$@"
        EOS
        chmod 0755, [bin/"conductor-background-task", bin/"conductor-background-output", bin/"conductor-background-cancel", bin/"conductor-background-batch"]
        pkgshare.install "skills", "commands", "config"
      end
    end
    if Hardware::CPU.arm? && Hardware::CPU.is_64_bit?
      url "https://github.com/Skyline-23/conductor-kit/releases/download/v0.1.7/conductor-kit_0.1.7_linux_arm64.tar.gz"
      sha256 "886616d963069b1cbff77019a9e872e7e6417c18a8a0a585798a6cd5a58fde86"
      def install
        bin.install "conductor"
        (bin/"conductor-background-task").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-task "$@"
        EOS
        (bin/"conductor-background-output").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-output "$@"
        EOS
        (bin/"conductor-background-cancel").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-cancel "$@"
        EOS
        (bin/"conductor-background-batch").write <<~EOS
          #!/bin/sh
          exec "#{bin}/conductor" background-batch "$@"
        EOS
        chmod 0755, [bin/"conductor-background-task", bin/"conductor-background-output", bin/"conductor-background-cancel", bin/"conductor-background-batch"]
        pkgshare.install "skills", "commands", "config"
      end
    end
  end

  def post_install
    system "#{bin}/conductor", "install", "--mode", "link", "--repo", "#{pkgshare}", "--force"
  end

  def caveats
    <<~EOS
      Skills/commands were linked into ~/.codex and ~/.claude during install.
      If you need to re-link:
        conductor install --mode link --repo #{pkgshare} --force
    EOS
  end
end
